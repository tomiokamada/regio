#include <stdio.h>
#include <stdlib.h>

int h[101];

int main(void) {
	int n,i, skip;
	scanf("%d", &n);
	for(i=0; i<n+1; i++) {
		scanf("%d", &h[i]);
	}

	for(skip=0; skip<n+1; skip++) {
		/* 実装してね */
	}
	return 0;
}
