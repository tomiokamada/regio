#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    int x, s=0;
    scanf("%d", &x);
    s += x;
    scanf("%d", &x);
    s += x;
    scanf("%d", &x);
    s += x;
    scanf("%d", &x);
    s += x;
    scanf("%d", &x);
    s += x;
    printf("%d\n", s);
    return 0;
}
