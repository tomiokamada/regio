#include <iostream>
using namespace std;
int main(void)
{
    int x0, x1, x2, x3, x4;
    cin >> x0;
    cin >> x1;
    cin >> x2 >> x3 >> x4;
    cout << (x0 + x1 + x2 + x3 + x4) << endl;
    return 0;
}
